# 介面
streamlit>=1.34.0  # st.pills 需要 1.34+(產業 pre-filter UI)

# 資料來源
finmind>=1.9.0
yfinance>=0.2.40
tqdm>=4.66.0  # finmind 1.9.x 漏寫的依賴,需補上
httpx>=0.27.0  # TWSE OpenAPI fallback HTTP client(雲端 SSL 比 requests 寬鬆)

# 資料處理
pandas>=2.2.0
numpy>=1.26.0

# 視覺化
plotly>=5.20.0

# ML 模型(短線勝率預測,RandomForestClassifier)
scikit-learn>=1.3.0
joblib>=1.3.0

# SHAP ML 解釋性(Top 3 feature 貢獻 — 告訴主公「為什麼這檔分數高」)
shap>=0.44.0

# LLM(個股頁公司資訊 description / uniqueness / moat 生成,用 Gemini 1.5 Flash)
# 註:Google 已將 google-generativeai 標 deprecated,等之後再 migrate google-genai
google-generativeai>=0.7.0

# 環境變數
python-dotenv>=1.0.0

# 開發工具
pytest>=8.0.0
ruff>=0.4.0
